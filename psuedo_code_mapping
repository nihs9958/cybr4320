Parse SAL program using Scala parser
For each KMedians operator expression in the program:
  Initialize a KMediansExp object with the field and k values from the expression, and the SAM memory
  Call the createOpString() method on the KMediansExp object to generate the SAM code for the operator
  Add the SAM code to the overall SAM program string
